Deep Learning Homework 3
The provided code is compatible with PyTorch 2.0 and Python 3.6.

Running the Code
To execute the code, follow these steps:

Download both the test and train versions of the Spoken-SQuAD datasets into the root directory.
Install the required packages if they aren't already installed.
Run the code by using the bash command in the terminal.
Make sure the datasets are downloaded and placed in the root folder before executing the code. Also, verify that all necessary packages are installed to prevent any compatibility issues.
